---
"techlead": patch
---

Add `-v`/`--version` flag to CLI showing package version and git commit id

The version information is now generated at build time and bundled into the distribution, ensuring it works correctly when installed via npm.
